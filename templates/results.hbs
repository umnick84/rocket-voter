{{#*inline "page"}}

<h1>Lunch Vote</h1>

<br />

Vote results:
<br />
    <table>
        {{#each frequency}}
            <tr>
            <td> {{ this.0 }} </td>
            <td> {{ this.1 }} </td>
            </tr>
        {{/each}}
    <table>

<br /><br />
Details:
<br />

    <table>
        {{#each votes}}
            <tr>
                <td> {{ this.username }} </td>
                <td> {{ this.place }} </td>
            </tr>
        {{/each}}
    <table>


{{/inline}}
{{~> (parent)~}}